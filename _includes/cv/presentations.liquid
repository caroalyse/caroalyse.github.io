<ul class="cv-entry-list card-text font-weight-light">
  {% for entry in entries %}
    <li class="cv-entry-item">
      <div class="cv-entry-grid">
        <div class="cv-entry-meta">
          {% if entry.date %}
            <span class="cv-meta-badge">{{ entry.date }}</span>
          {% endif %}
          {% if entry.location %}
            <p class="cv-entry-location">
              <i class="fa-solid fa-location-dot iconlocation"></i>
              {{ entry.location }}
            </p>
          {% endif %}
        </div>

        <div class="cv-entry-content">
          <h6 class="cv-entry-title">{{ entry.title }}</h6>
          {% if entry.authors %}
            <div class="cv-entry-subtitle">{{ entry.authors | join: ', ' }}</div>
          {% endif %}
          {% if entry.summary %}
            <div class="cv-entry-secondary">
              {{ entry.summary | markdownify | remove: '<p>' | remove: '</p>' }}
            </div>
          {% endif %}
        </div>
      </div>
    </li>
  {% endfor %}
</ul>
